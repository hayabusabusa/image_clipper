import 'package:flutter/material.dart';

void main() => ImageClipperApp();

class ImageClipperApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    MaterialApp(
      title: 'Clipper',
    );
  }
}